╔════════════════════════════════════════════════════════════════════════════╗
║                  🐳 PCAP Analyzer - Docker Quick Reference                 ║
╚════════════════════════════════════════════════════════════════════════════╝

📦 INSTALLATION (One Command)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Linux/macOS:  ./start.sh
  Windows:      start.bat

🚀 DOCKER COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Start:        docker-compose up -d
  Stop:         docker-compose stop
  Restart:      docker-compose restart
  Logs:         docker-compose logs -f
  Status:       docker-compose ps
  Remove:       docker-compose down
  Update:       git pull && docker-compose up -d --build
  Clean All:    docker-compose down -v

🌐 ACCESS POINTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Frontend:     http://localhost
  Backend:      http://localhost:8000
  API Docs:     http://localhost:8000/docs
  ClickHouse:   localhost:9000 (native), localhost:8123 (http)

📊 SERVICES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ┌─────────────┬──────┬─────────────────────────┐
  │ Service     │ Port │ Description             │
  ├─────────────┼──────┼─────────────────────────┤
  │ Frontend    │ 80   │ React UI (Nginx)        │
  │ Backend     │ 8000 │ FastAPI + PyShark       │
  │ ClickHouse  │ 9000 │ Database (Native)       │
  │ ClickHouse  │ 8123 │ Database (HTTP)         │
  └─────────────┴──────┴─────────────────────────┘

📁 KEY FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  docker-compose.yml     - Service orchestration
  Dockerfile.backend     - Backend container config
  Dockerfile.frontend    - Frontend container config
  .env                   - Environment variables (create from .env.example)
  nginx.conf             - Nginx configuration
  start.sh / start.bat   - Quick start scripts

📝 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  README.md              - Project overview & quick start
  INSTALLATION.md        - Detailed installation guide
  DOCKER_GUIDE.md        - Docker-specific documentation
  DEPLOYMENT_SUMMARY.md  - Complete deployment overview

🔧 CONFIGURATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. Copy template:      cp .env.example .env
  2. Generate secret:    python3 -c "import secrets; print(secrets.token_urlsafe(32))"
  3. Edit .env:          nano .env
  4. Set JWT_SECRET_KEY: <paste generated secret>

🔒 SECURITY CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ☐ Change JWT_SECRET_KEY in .env
  ☐ Use HTTPS in production (SSL certificates)
  ☐ Configure firewall rules
  ☐ Regular backups
  ☐ Keep Docker images updated

🎯 FIRST-TIME SETUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. git clone https://github.com/jeevanelton/pcap.git
  2. cd pcap
  3. ./start.sh (or start.bat on Windows)
  4. Open http://localhost
  5. Click "Register" to create account
  6. Start analyzing PCAP files!

🛠️ TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Port in use:           Change ports in docker-compose.yml
  Can't connect:         docker-compose logs <service>
  Out of space:          docker system prune -a
  Reset everything:      docker-compose down -v && ./start.sh

💾 BACKUP & RESTORE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Backup uploads:        tar -czf backup.tar.gz backend/uploads/
  Backup database:       docker-compose exec clickhouse clickhouse-client --query "BACKUP DATABASE pcap_db"
  List volumes:          docker volume ls

📊 MONITORING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  View logs:             docker-compose logs -f [service]
  Resource usage:        docker stats
  Container status:      docker-compose ps
  Network inspect:       docker network inspect pcap_default

🚀 PRODUCTION DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. Set strong JWT_SECRET_KEY
  2. Configure SSL (nginx reverse proxy)
  3. Set resource limits in docker-compose.yml
  4. Enable auto-restart: restart: unless-stopped
  5. Set up monitoring (Prometheus/Grafana)
  6. Configure backups (cron jobs)

🎓 USAGE WORKFLOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Register → Login → Create Project → Upload PCAP → 
  View Dashboard → Click Feature Cards → Export Data

📋 FEATURES AVAILABLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ DNS Query Analysis
  ✓ HTTP Traffic Analysis
  ✓ TLS/SSL Sessions
  ✓ Open Ports Detection
  ✓ IP Connections
  ✓ ARP Traffic (with spoofing detection)
  ✓ SMB File Sharing
  ✓ Network Graph Visualization
  ✓ Flow Diagram
  ✓ Geographic Map
  ✓ Protocol Distribution
  ✓ Traffic Timeline

🔗 USEFUL LINKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GitHub:      https://github.com/jeevanelton/pcap
  Issues:      https://github.com/jeevanelton/pcap/issues
  Docker Docs: https://docs.docker.com

╔════════════════════════════════════════════════════════════════════════════╗
║  Created: November 7, 2025  |  Version: 1.0.0  |  License: MIT            ║
╚════════════════════════════════════════════════════════════════════════════╝
